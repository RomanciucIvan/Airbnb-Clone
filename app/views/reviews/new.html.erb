<h1>New review</h1>
<%= simple_form_for [@apartment, @review] do |f| %>
  <%= f.input :content %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
  <div>
    <%= link_to "Back to apartment", apartment_path(@apartment), class: "btn btn-secondary" %>
  </div>
<% end %>



