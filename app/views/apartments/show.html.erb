<%= render @apartment %>
<div>

  <% if policy(@apartment).edit? %>
  <%= link_to "Edit", edit_apartment_path(@apartment) %> |
  <% end %>

  <%= link_to "Back to apartments", apartments_path %>

  <% if policy(@apartment).destroy? %>
  <%= button_to "Delete", @apartment, method: :delete %>
  <% end %>
</div>
